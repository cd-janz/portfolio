---

---
<figure class="hero_sphere">
  <div class="absolute top-[25%] sm:top-[1%] md:top-[15%] lg:top-[10%] w-full flex flex-col items-center">
    <slot />
  </div>
</figure>
<style is:inline>
  .hero_sphere{
    position: absolute;
    width: 150dvw;
    height: 150dvw;
    border: 2px solid var(--border);
    background: linear-gradient(180deg, var(--border) 0%, var(--surface) 30%);
    top: 30%;
    border-radius: 100%;
    display: flex;
    justify-content: center;
  }
  @media (min-width: 1536px){
    .hero_sphere{
      width: 100dvw;
      height: 120dvh;
    }
  }
</style>
